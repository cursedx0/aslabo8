apiVersion: apps/v1
kind: Deployment
metadata:
  name: front-web
  labels:
    app: front-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: front-web
  template:
    metadata:
      labels:
        app: front-web
    spec:
      containers:
      - name: front-web
        image: cursedx0/aslabo8-web:latest
        ports:
        - containerPort: 5000
